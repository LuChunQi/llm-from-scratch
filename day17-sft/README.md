# Day 17: SFT（监督微调）— 让模型从"会说话"到"会听话"

## 🔗 上节回顾

昨天我们完成了预训练的全流程：从原始文本到 Tokenizer 编码，从随机权重的 Transformer 到能生成莎士比亚风格文本的模型。我们亲眼看到了模型如何分阶段"涌现"出语言能力——先学字母频率，再学单词，最后学句子结构。

但昨天结尾我们留了一个问题：预训练模型只是学会了"说类似训练数据的话"——给它一个开头，它就自顾自地续写。你让它"总结一下这篇文章"，它可能完全不理你。怎么让模型"听懂指令"呢？

今天我们就来解决这个问题——**SFT（Supervised Fine-Tuning，监督微调）**，让模型从"会说话"进化到"会听话"。

---

## 🎭 从"话痨"到"助手"：为什么需要 SFT

> ❓ **问题**：预训练之后的模型能生成流畅的文本，为什么还不能当助手用？

想象你雇佣了一个刚从文学系毕业的学生。他读过几万本书，文笔极好，但——

你说："帮我总结一下这份报告。"
他回答："报告的内容涵盖了多个方面，其中第一部分讨论了……"然后开始了长达三页的续写。

你说："1+1等于几？"
他回答："1+1等于几，这是一个古老的数学问题。从毕达哥拉斯学派的视角来看……"

他不是不会说话，他是**不会听话**。他不知道"你说了什么"和"他该怎么回应"之间的对应关系。

预训练模型也是一样的。它学到了语言的统计规律，但它从来没有学过"**人类指令 → 期望回答**"这种映射。

SFT 要做的事情就是：

```
预训练模型：输入开头 → 续写文本（自顾自地写）
SFT 模型：  输入指令 → 回答问题（听懂了再回答）

"请把下面的句子翻译成英文：我喜欢猫"
预训练模型 → "请把下面的句子翻译成英文：我喜欢猫。翻译是人类文明中……"（继续写）
SFT 模型   → "I like cats."
```

**SFT 就是教模型"你该怎么回答人类的问题"。**

---

## 📚 SFT 的核心思路：用"问答对"做教科书

> ❓ **问题**：怎么教模型"听指令"？具体用什么数据？

预训练用的是"无标注数据"——网上随便爬的文本，没有人标记。模型只是学着"预测下一个词"。

SFT 用的是**有标注数据**——人工写好的"指令-回答"配对。每一条数据都在告诉模型：**人家这么问你，你应该这么回答**。

### SFT 数据长什么样？

```
示例 1：
  指令: "什么是光合作用？"
  回答: "光合作用是植物利用阳光、水和二氧化碳制造养分的过程。具体来说……"

示例 2：
  指令: "把下面的句子翻译成英文：今天天气很好。"
  回答: "The weather is nice today."

示例 3：
  指令: "写一首关于春天的短诗。"
  回答: "春风拂面柳丝长，\n桃杏花开满院香。\n燕子归来寻旧巢，\n一池碧水映斜阳。"

示例 4：
  指令: "8 × 7 = ?"
  回答: "8 × 7 = 56"
```

每一条都是"**人类指令 + 期望回答**"。模型在这些数据上微调，就学会了"面对不同类型的指令，给出合适格式的回答"。

### SFT 数据从哪来？

早期（GPT-3 时代），这些数据需要**人工一条一条写**，非常昂贵。OpenAI 雇佣了大约 40 名标注员，花了几个月写了几万条高质量的指令-回答对。

后来社区找到了更聪明的方法：

| 数据集 | 方法 | 规模 |
|--------|------|------|
| InstructGPT 数据 | 人工标注 | ~13K 条 |
| Alpaca | 用 GPT-3.5 生成指令-回答对 | 52K 条 |
| Self-Instruct | 模型自己生成指令 | 82K 条 |
| OpenAssistant | 众包标注 | 161K 条 |
| ShareGPT | 用户分享的 ChatGPT 对话 | 数十万条 |
| UltraChat | GPT-3.5 生成多轮对话 | 1.47M 条 |

现在的标准做法：**用强模型（如 GPT-4）生成大量指令-回答对，再用这些数据微调你的模型**。这叫"**蒸馏数据**"——从强模型中"蒸馏"出训练数据来教弱模型。

---

## 🔧 SFT 的技术实现：比预训练简单得多

> ❓ **问题**：SFT 和预训练在技术上有什么区别？

答案可能会让你惊讶：**几乎没有区别**。

预训练和 SFT 的**训练过程完全一样**：

```
1. 把文本编码成 token 序列
2. 模型做前向传播，预测每个位置的下一个 token
3. 用交叉熵损失比较预测和真实值
4. 反向传播，更新参数
```

唯一的区别在于**数据格式**：

```
预训练数据：
  "从前有座山，山里有座庙，庙里有个老和尚……"
  → 模型学着续写

SFT 数据：
  "<指令> 什么是光合作用？ <回答> 光合作用是……"
  → 模型学着在"指令"之后生成"回答"
```

我们用一个特殊的**格式化模板（Chat Template）**把指令和回答拼在一起：

```
模板：
  "<|im_start|>user\n{instruction}<|im_end|>\n<|im_start|>assistant\n{response}<|im_end|>"

实际数据：
  "<|im_start|>user
   什么是光合作用？
   <|im_end|>
   <|im_start|>assistant
   光合作用是植物利用阳光……
   <|im_end|>"

模型看到这段文本，学习的是：
  - 看到 "<|im_start|>user" 后面跟着问题
  - 看到 "<|im_end|>" 之后应该生成回答
```

这就是 ChatGPT 为什么能区分"用户说的话"和"自己要生成的回答"——**因为训练数据中用特殊 token 标记了角色**。

### 最重要的技巧：只对"回答部分"计算损失

这是一个微妙但关键的细节：

```
完整序列：<指令token> 什么是光合作用？ <回答token> 光合作用是……

损失计算：
  ❌ 错误做法：对整个序列计算损失
     → 模型会学着预测用户的问题，浪费时间

  ✅ 正确做法：只对"回答"部分的 token 计算损失
     → 模型只学着生成回答，不学着生成问题
```

这叫**损失掩码（Loss Masking）**：

```python
# labels 中，指令部分设为 -100（PyTorch 忽略的值），回答部分正常
labels = [-100, -100, -100, ..., -100,  # 指令部分：不计算损失
          token_id, token_id, ...]        # 回答部分：正常计算损失
```

`-100` 在 PyTorch 的 `CrossEntropyLoss` 中是 `ignore_index` 的默认值——遇到它就跳过，不计算损失。

---

## 📊 SFT 的数据格式深入

> ❓ **问题**：多轮对话怎么做 SFT？

真实场景中，用户和助手通常有多轮对话：

```
用户：今天天气怎么样？
助手：请问你在哪个城市？
用户：北京
助手：北京今天晴，最高温度 28°C。
```

在 SFT 中，我们把整个对话历史拼在一起，但**只在助手的回答部分计算损失**：

```
<user>今天天气怎么样？</user>
<assistant>请问你在哪个城市？</assistant>    ← 计算损失
<user>北京</user>
<assistant>北京今天晴，最高温度 28°C。</assistant>    ← 计算损失
```

每一轮助手回答，模型都需要参考前面的全部对话历史来生成合适的回复。这就是为什么 LLM 能"记住"你之前说的话——**训练时它就学会了看上下文**。

---

## 🧪 SFT 的超参数：小数据、小学习率

> ❓ **问题**：SFT 的训练设置和预训练有什么不同？

| 参数 | 预训练 | SFT |
|------|--------|-----|
| 数据量 | 1TB+ | 10K ~ 100K 条 |
| 学习率 | 1e-4 ~ 6e-4 | 1e-5 ~ 5e-5（小 10 倍） |
| 训练轮数 | 1 epoch | 3 ~ 5 epochs |
| 训练时间 | 几周到几个月 | 几小时到几天 |
| 训练成本 | 百万美元级 | 几十到几百美元 |

为什么学习率要小得多？

因为预训练模型已经"学会了语言"——我们不想破坏这些知识，只是想在上面"微调"出"听指令"的能力。如果学习率太大，模型会**灾难性遗忘**（Catastrophic Forgetting）——学会了听指令，却忘了怎么说人话。

类比：预训练是大学四年通识教育，SFT 是入职培训。入职培训应该是"在已有知识基础上学习新技能"，而不是"把脑子清空重新来"。

---

## 🏗️ SFT 的完整流程

> ❓ **问题**：从原始数据到微调完成，具体步骤是什么？

```
┌─────────────────────────────────────────────────┐
│  Step 1: 准备 SFT 数据                           │
│  收集/生成 (指令, 回答) 配对                       │
│  格式化为统一模板                                 │
└────────────────────┬────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│  Step 2: Tokenize + 构建损失掩码                   │
│  把文本变成 token ID 序列                          │
│  标记哪些位置是"回答"（需要计算损失）              │
└────────────────────┬────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│  Step 3: 加载预训练模型                            │
│  不是随机初始化！从预训练权重开始                   │
└────────────────────┬────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│  Step 4: 训练                                     │
│  前向 → 只在回答部分计算损失 → 反向 → 更新参数     │
│  小学习率，少量 epoch                              │
└────────────────────┬────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│  Step 5: 评估 & 保存                              │
│  测试模型是否学会了听指令                          │
│  保存最终权重                                     │
└─────────────────────────────────────────────────┘
```

---

## 💡 SFT 的局限性

> ❓ **问题**：SFT 就够了吗？为什么后面还需要 RLHF/DPO？

SFT 教会了模型"听指令"，但有一个根本问题：**它学到的是模仿人类写的回答，而不是生成真正"好"的回答**。

举个例子：

```
指令: "证明 1+1=2"

SFT 数据中的回答（人类写的）:
  "这是一个简单的问题。1+1=2，因为当我们把一个苹果和另一个苹果放在一起时……"

更好的回答（更精确、更有用）:
  "在 Peano 公理体系中，1+1=2 的证明如下：
   定义 S(n) 为 n 的后继……"
```

SFT 会学到人类标注者写的那个版本——因为它在训练数据里。但人类标注者写的未必是最好的回答。

更严重的问题是**人类偏好**。同样的指令，可能有多种合理的回答：

```
指令: "用简单的话解释量子力学"

回答 A: 用比喻和日常例子解释（通俗易懂）
回答 B: 用公式和严谨定义解释（专业准确）
```

哪个更好？取决于用户。SFT 没法学这种"偏好"——因为训练数据只有一种回答。

**这引出了后面的 RLHF 和 DPO：用人类的偏好反馈来训练模型，让它不只是模仿，而是优化"人类觉得好"的目标。**

---

## 🧩 代码实现：从零实现 SFT

今天的 `sft.py` 包含：

1. **SFT 数据构造**：创建多种类型的指令-回答对（问答、翻译、总结、数学等）
2. **Chat Template 格式化**：用特殊 token 标记角色，构建损失掩码
3. **损失掩码机制**：只在助手回答部分计算损失
4. **完整 SFT 训练循环**：从预训练模型加载 → 微调 → 保存
5. **对比实验**：预训练模型 vs SFT 模型面对指令时的不同表现
6. **灾难性遗忘演示**：展示学习率过大时模型会"忘掉"已学知识

运行方式：
```bash
python3 sft.py
```

你将看到同一个模型，在 SFT 前后面对指令的截然不同表现。

---

## 🔑 关键洞察

1. **SFT 的本质是"指令-回答"的监督学习**——教模型"听到这种问题，就那样回答"
2. **技术实现和预训练几乎一样**——区别只在数据格式和损失掩码
3. **Chat Template 是灵魂**——用特殊 token 标记角色，让模型区分"用户说的"和"我要回答的"
4. **只对回答部分计算损失**——这是 SFT 最关键的技巧
5. **小学习率防遗忘**——SFT 是"微调"不是"重训"
6. **SFT 学的是模仿，不是优化**——它学会了"像人类那样回答"，但不一定是最优回答。这引出了后面的 RLHF

---

## 📝 一句话总结

> SFT 就是拿预训练好的模型，用"指令-回答"配对数据做监督微调——技术实现和预训练几乎一样（下一个词预测），但通过 Chat Template 区分角色、损失掩码只训练回答部分、小学习率防止灾难性遗忘，让模型从"自顾自地续写"变成"听懂指令再回答"。

## 📖 关键术语速查

| 术语 | 含义 |
|------|------|
| **SFT（Supervised Fine-Tuning）** | 用标注好的指令-回答数据微调预训练模型 |
| **Chat Template** | 用特殊 token 标记用户/助手角色的文本格式模板 |
| **损失掩码（Loss Masking）** | 只在特定部分（助手回答）计算损失，其余位置设为 -100 |
| **灾难性遗忘（Catastrophic Forgetting）** | 学习新知识时忘掉旧知识的的现象 |
| **指令微调（Instruction Tuning）** | SFT 的另一种叫法，强调"学会听指令" |
| **蒸馏数据（Distillation Data）** | 用强模型生成的训练数据来教弱模型 |
| **Epoch** | 完整遍历一次训练数据 |
| **多轮对话（Multi-turn）** | 用户和助手之间的连续对话，模型需要参考历史上下文 |

---

> 🤔 **今天留下的悬念**：SFT 让模型学会了"听指令"，但它只是模仿人类标注者写的回答——而人类标注者写的未必是最好的。怎么让模型学会"生成人类更喜欢的回答"呢？明天的主题——**RLHF（基于人类反馈的强化学习）**，教模型不只是模仿，而是真正优化"人类觉得好"这个目标。

**下节预告**：Day 18 — RLHF，让模型学会"讨好"人类。

---

*本课程代码开源于 [GitHub](https://github.com/LuChunQi/llm-from-scratch)，欢迎 Star ⭐*
